module.exports = {
    style: {
        postcss: {
            plugins: [
                require('autoprefixer'),
                // Add more PostCSS plugins if needed
            ],
        },
    },
};
